<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/orderAdmin.css" />
    <title>Document</title>
    <style>
           .edits {
            display: flex;
            justify-content: center;
            width: 100%;
            height: 100%;
            /* border: 5px solid red; */
            margin-top: 80px;
        }

        .edits h1 {
            font-size: 40px;
            font-weight: 500;
        }

        .edits form {
            width: 400px;
            height: 400px;
            border: 10px solid darkblue;
            padding: 20px;
            text-align: center;
        }

        .edits form div {
            margin: 10px;
        }

        .edits form div input{
            margin-top: 10px;
        }
        .edits form div label {
            font-size: 20px;
        }

        .edits form .submitUpdate {
            margin-top: 50px;
        }
        .edits form div button {
            width: 150px;
            background: darkblue;
            padding: 10px;
            border-radius: 20px;
            color: #fff;
            cursor: pointer;
            outline: none;
            border: none;
            transition: all ease-in-out 0.8s;
        }

        .edits form div button:hover {
            background: blue;
        }

        hr {
            width: 400px;
            background: darkblue;
            border: 2px solid darkblue;
        }
    </style>
</head>
<body>
    <div class="edits">
     <form action="/all_order/update" method="post">
        <h1>Edit Order Information</h1>
        

        <div class="">
            <input type="hidden" name="id" value="<%= order[0].id %>"/>
            <p><%= order[0].fname +' '+ order[0].lname %></p>
            <label for="">Trainee
                <input type="text" name="trainee_id" id="" value="<%= order[0].fname +' '+ order[0].lname %>"/>
        </div>

        <div class="">
            <label for="">Change Meal Option
                <input type="text" name="meal_option_id" value="<%= order[0].option_name %>" />
            </label>

            <label for="">Order Date
                <input type="date" name="date" value="<%= new Date(order[0].date).toLocaleDateString('fr-CA') %>"/>
            </label>
           
        </div>

        <div class="submitUpdate">
            <button type="submit">Submit Changes</button>
        </div>

        
    </form>
    </div>
</body>
</html>